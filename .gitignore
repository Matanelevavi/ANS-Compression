# Build artifacts
*.exe
*.o
*.obj

# Compression outputs and temporary artifacts
*.rans
*.my_ans
*.ref_arith
*.restored
*.bin

# Restored sample files produced by the encryption test suite
results/encryption_tests/*/

# Python
__pycache__/
*.pyc

# Large corpus files (~300MB total). Not yet removed from tracking -
# see files/50MFiles/download_corpus.py. This pattern only takes
# effect once the files are untracked (git rm --cached), planned for
# just before the first push.
# files/50MFiles/*.50MB

# Editor / OS files
.vscode/
Thumbs.db
.DS_Store
