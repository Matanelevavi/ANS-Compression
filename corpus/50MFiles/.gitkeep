This directory is intentionally kept empty in the repository.

Large benchmark files (50MB datasets) are not stored in Git to keep the repository lightweight.

To run the large-file compression tests:
1. Go to the folder: "zip files 50M".
2. Extract the compressed dataset files.
3. Place the extracted files in this directory.

After extraction, this folder should contain the 50MB benchmark files used for large dataset compression tests.
